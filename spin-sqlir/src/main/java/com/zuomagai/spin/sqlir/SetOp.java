package com.zuomagai.spin.sqlir;

public enum SetOp {
    UNION,
    UNION_ALL,
    MINUS,
    MINUS_DISTINCT,
    MINUS_ALL,
    EXCEPT,
    EXCEPT_ALL,
    EXCEPT_DISTINCT,
    INTERSECT,
    INTERSECT_ALL,
    INTERSECT_DISTINCT,
    DISTINCT
}
